# =============================================================================
# Argus MCPS — Build do Bundle OTA (RAUC)
# Uso: kas-container build kas/bundle.yml
# =============================================================================
header:
  version: 14
  includes:
    - repo: this
      path: kas/base.yml

target: infusion-bundle
